import React from 'react';

import MatIcon from 'react-native-vector-icons/MaterialCommunityIcons';

const Icon = ({name, size, color}) => {
  return <MatIcon name={name} size={size} color={color} />;
};

export default Icon;
